const configs = {
  title: { 
    opacity: 0, 
    y: 20,           // ← 调整位移距离
    duration: 0.7     // ← 调整时长
  },
  content: { 
    opacity: 0, 
    y: 24,           // ← 调整位移距离  
    duration: 0.6    // ← 调整时长
  },
  image: { 
    opacity: 0, 
    y: 16,           // ← 调整位移距离
    duration: 0.55   // ← 调整时长
  }
};